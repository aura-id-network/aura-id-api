🎴 AURA Cards API
Aura Cards API — это профессиональное серверное решение для управления цифровыми коллекционными картами, идентификации пользователей через Telegram и интеграции механик "Token Gating". Сервер обеспечивает полный цикл работы с картами: от генерации и хранения до верификации прав доступа на сторонних ресурсах.

📋 Содержание
Функциональность

Технологический стек

Установка и запуск

Конфигурация

API Документация

Структура проекта

🚀 Функциональность
Система идентификации: Привязка карт к Telegram ID пользователя

Token Gating: API для проверки наличия карт для доступа к контенту

Коллекции: Поддержка авторских коллекций и динамическое ценообразование

Access Keys: Уникальные ключи доступа для безопасной проверки

Автоматические миграции: Встроенное версионирование БД

CORS поддержка: Интеграция с веб-сайтами и внешними сервисами

🛠 Технологический стек
Язык: Python 3.8+

Фреймворк: Flask

База данных: SQLite

Утилиты: python-dotenv, flask-cors, Pillow (для генерации карт)

⚙️ Установка и запуск
1. Клонирование и настройка окружения
bash
# Клонирование репозитория
git clone https://github.com/aura-id-network/aura-id-api.git
cd aura-id-api

# Создание виртуального окружения
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows

# Установка зависимостей
pip install -r requirements.txt
2. Настройка конфигурации
Создайте файл .env в корне проекта:

env
BOT_TOKEN=your_telegram_bot_token
DATABASE_URL=sqlite:///aura_pro.db
API_BASE_URL=https://144.31.164.141.sslip.io
FLASK_HOST=0.0.0.0
FLASK_PORT=20196
3. Инициализация базы данных
bash
python -c "from database import init_db; init_db()"
4. Запуск API сервера
bash
python api_server.py
Сервер запустится на порту 20196 и будет доступен по адресу https://144.31.164.141.sslip.io.

5. Запуск как сервис (Linux)
bash
# Создание systemd сервиса
sudo nano /etc/systemd/system/aura-api.service
Добавьте конфигурацию:

ini
[Unit]
Description=AURA Cards API Service
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/root/aurabot
Environment=PATH=/usr/local/bin:/usr/bin:/bin
ExecStart=/usr/bin/python3 /root/aurabot/api_server.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
Запуск сервиса:

bash
sudo systemctl daemon-reload
sudo systemctl enable aura-api
sudo systemctl start aura-api
sudo systemctl status aura-api
🔧 Конфигурация
Основные параметры (config.py):
python
BOT_TOKEN = "7993006550:AAHvqPRJzkmwfWrL1lbDZShMc2c0VEh3lQQ"  # Telegram Bot Token
ADMIN_IDS = [6829778011]  # ID администраторов
DATABASE_URL = "sqlite:///aura_pro.db"  # Путь к базе данных
CARD_CREATION_PRICE = 1  # Цена создания карты в Telegram Stars
API_BASE_URL = "https://144.31.164.141.sslip.io"  # Публичный URL API
FLASK_PORT = 20196  # Порт для Flask сервера
Цвета гравировки:
python
ENGRAVING_COLORS = {
    "white": "#FFFFFF",
    "bronze": "#CD7F32", 
    "gold": "#FFD700"
}
📚 API Документация
API доступно по адресу: https://144.31.164.141.sslip.io/api

Основные эндпоинты:
1. Проверка работоспособности
text
GET /api/health
Возвращает статус сервера и список доступных эндпоинтов.

2. Проверка карт пользователя
text
POST /api/check-card
Проверяет наличие карт у пользователя.

Тело запроса:

json
{
    "telegram_id": 123456789
}
3. Проверка коллекции
text
POST /api/check-collection
Проверяет наличие карт из указанной коллекции у пользователя.

Тело запроса:

json
{
    "telegram_id": 123456789,
    "collection_access_key": "ABCD-EFGH-IJKL"
}
4. Получение всех коллекций
text
GET /api/collections
Возвращает список всех публичных коллекций.

5. Получение карты по ключу
text
GET /api/cards/{access_key}
Возвращает информацию о карте по access key.

6. Получение карт пользователя
text
GET /api/user/{telegram_id}/cards
Возвращает все карты пользователя.

Полная документация:
text
GET /api/docs
Возвращает полную документацию по всем эндпоинтам.

📁 Структура проекта
text
aurabot/
├── config.py              # Конфигурация приложения
├── database.py            # Модели и работа с БД
├── api_server.py          # Flask API сервер
├── requirements.txt       # Зависимости Python
├── .env                  # Переменные окружения
├── cards/                # Директория для изображений карт
├── pids/                 # PID файлы процессов
├── logs/                 # Логи приложения
└── aura_pro.db           # База данных SQLite
🔒 Безопасность
Access Keys: Каждая карта имеет уникальный ключ доступа

Валидация данных: Все входящие запросы валидируются

CORS: Настроены безопасные политики CORS

SQL-инъекции: Используются параметризованные запросы

🐛 Отладка и логи
Просмотр логов:
bash
# Если используется systemd
sudo journalctl -u aura-api -f

# Или просмотр файла логов
tail -f /root/aurabot/logs/api_server.log
Проверка состояния БД:
bash
sqlite3 aura_pro.db ".tables"
sqlite3 aura_pro.db "SELECT * FROM users LIMIT 5;"
📄 Лицензия
Проект распространяется под проприетарной лицензией. Все права защищены.
